{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "CMake: Launch active target",
          "type": "cppvsdbg",
          "request": "launch",
          "program": "${command:cmake.launchTargetPath}",
          "cwd": "${workspaceFolder}",
          "console": "externalTerminal"
      }
  ]
}

